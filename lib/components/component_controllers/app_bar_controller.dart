// appbar_controller.dart
import 'package:flutter/material.dart';

class AppBarController {
  // El controlador maneja eventos y lógica de negocio
  void handleBackPressed(BuildContext context) {
    Navigator.pop(context);
  }
}
